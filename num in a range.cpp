/*check if a number is within range 10 to 50*/
#include <stdio.h>
int main () {
	int num
	printf ("enter a number:");
	scanf ("%d", &num);
	if (num>=10 && num<=50);
	 printf("%d is within the range of 10 to 50\n", num);
	 else
	 printf("%d is NOT in the range of 10 to 50\n", num);
	 return 0;
}
